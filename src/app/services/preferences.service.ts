//import { Injectable } from '@angular/core';
//import { Preferences } from '@capacitor/preferences';

//@Injectable({
  //providedIn: 'root',
//})
//export class PreferencesService {
 // async saveUnitPreference(unit: string) {
 //   await Preferences.set({ key: 'temp_unit', value: unit });
 // }

//  async getUnitPreference() {
 //   const unit = await Preferences.get({ key: 'temp_unit' });
 //   return unit.value || 'Celsius'; // Default to Celsius
//  }
//}
